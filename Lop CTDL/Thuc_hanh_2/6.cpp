#include <bits/stdc++.h>
using namespace std;
int n;
int a[101][101]
void testcase(){
    cin >> n;
    for (int i = 0;i<n;i++){
        for (int j = 0;j<n;j++)
            cin >> a[i][j];
    }
    string res;
    queue<string> q;
    int i = 0, j = 0;
    q.push(a[0][0]);
    while(q.size()){
        string s = q.front();
        q.pop();
        string t1 = q+
    }
}
int main()
{
    int x = 1;
    while (x--)
        testcase();
}